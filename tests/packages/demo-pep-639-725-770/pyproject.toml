#: schema ./pep725+pyproject.schema.json

# pyproject.toml
[build-system]
requires = ["meson-python>=0.15.0"]
build-backend = "mesonpy"

[project]
name = "demo_pep_639_725_770"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []
# PEP 639:
license = 'MIT OR BSD-3-Clause'
license-files = ['LICENSES/*']

# TODO: PEP 770: Software Bill-of-Materials (SBOM)
# are added manually to .dist-info in meson.build
# See https://github.com/mesonbuild/meson-python/issues/763

# PEP 725:
[external]
build-requires = [
    "dep:virtual/compiler/c",
    "dep:generic/pkg-config",
]
host-requires = [
    "dep:generic/gmp",
]
# Items below just added for schema demonstration purposes
dependencies = []

[external.optional-build-requires]
extra = []

[external.optional-host-requires]
extra = []

[external.optional-dependencies]
extra = []

